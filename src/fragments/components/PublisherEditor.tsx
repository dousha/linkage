import React from 'react';
import { ResourceType } from '../../models/ResourceType';

export interface PublisherEditorProps {
	resourceType: ResourceType;
}

export default function PublisherEditor(props: PublisherEditorProps) {
	return (<>

	</>);
}
